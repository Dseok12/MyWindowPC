$('.bxslider li').bxSlider({
  mode: 'vertical',
  randomStart: false,
	autoHover: false,
	autoDelay: 0,
	auto: true,
	pause: 3000,
	controls: true,
	pager: false,
	touchEnabled: (navigator.maxTouchPoints > 0)
});