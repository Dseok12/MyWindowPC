extends base.pug

block content
  if errorMessage
    span=errorMessage
  form(method="POST")
    span username
      input(placeholder="username", name="username", type="username", required)
    span password
      input(placeholder="password", name="password", type="password", required)
    input(type="submit", value="Login")
    a(href="/user/github/start") 깃허브 로그인 &rarr;
    hr
    div
      span 계정이 없으신가요?
      a(href="/join") Create Now &rarr;